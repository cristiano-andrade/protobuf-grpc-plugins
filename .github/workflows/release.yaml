name: Release Plugins
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  proto-gen-release:
    strategy:
      matrix:
        lang: [ python ]
        os: [ ubuntu, macos ]
    env:
      LANG: ${{ matrix.lang }}
      OS: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: grpc/grpc
      - name: Cache Bazel
        if: runner.os == 'Linux'
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/bazel
          key: ${{ runner.os }}-bazel-${{ hashFiles('.bazelversion', '.bazelrc', 'WORKSPACE', 'WORKSPACE.bazel', 'MODULE.bazel') }}
          restore-keys: |
            ${{ runner.os }}-bazel-
      - name: Cache Bazel
        uses: actions/cache@v3
        if: runner.os == 'macOS'
        with:
          path: |
            /private/var/tmp/_bazel_runner/
          key: ${{ runner.os }}-bazel-${{ hashFiles('.bazelversion', '.bazelrc', 'WORKSPACE', 'WORKSPACE.bazel', 'MODULE.bazel') }}
          restore-keys: |
            ${{ runner.os }}-bazel-
      - name: Build
        run: |
          echo "Building..."
          bazel build src/compiler:grpc_${{ env.LANG }}_plugin --enable_platform_specific_config
      - name: Package
        run: |
          cd bazel-bin/src/compiler/
          cp grpc_${{ env.LANG }}_plugin protoc-gen-grpc-${{ env.LANG }}
          tar czf protoc-gen-grpc-${{ env.LANG }}-${{ env.OS }}.tar.gz protoc-gen-grpc-${{ env.LANG }}
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: bazel-bin/src/compiler/protoc-gen-grpc-${{ env.LANG }}-${{ env.OS }}.tar.gz
          body: |
            protoc-gen-grpc-${{ env.LANG }}-${{ env.OS }}.tar.gz
